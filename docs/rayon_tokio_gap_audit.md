# Rayon/Tokio Gap Audit

## Scope

This audit covers the active unified-scheduler comparison scope:

- Public result-bearing task handles: `Moirai::spawn_fn`, `Moirai::spawn_async`, and `TaskHandle::join`.
- Completion-only parallel work: `Moirai::scope`.
- Indexed data-parallel work: `Moirai::for_each_indexed` and `Moirai::map_reduce_indexed`.
- Mixed scheduler work: completion-only sync fan-out, async result handles, and indexed reduction in one runtime.
- Batch quiescence: `Moirai::join` and `Moirai::has_work`.
- Transport receive memory behavior: `ArchiveView` over transport-owned bytes.
- Async timeout composition: `Timeout<F>` stores the concrete future inline.
- Async timer cancellation: `TimerWheel` tracks cancelled timer IDs lazily and verifies that cancelled expired timers do not wake.
- Native async I/O extension futures: `AsyncReadExt::read_exact` and `AsyncWriteExt::shutdown` borrow caller-owned buffers/writers directly, preserve partial read progress on cancellation, and are covered by value tests.
- Feature-gated Tokio I/O trait compatibility: transparent `TokioCompat<T>` and `MoiraiCompat<T>` wrappers preserve byte semantics in both directions, with `async_io_compat_comparison` measuring native extension futures against Tokio extension traits over the same in-memory readers and writers.
- Async file and directory facade: `moirai_async::fs::{read, write, append, metadata, rename, remove_file, copy}` have value-checked 64 KiB Tokio file facade read, write, append, metadata, rename, remove, and copy comparisons in `async_fs_comparison`; `moirai_async::fs::{create_dir, create_dir_all, remove_dir, remove_dir_all}` have value-checked Tokio directory facade comparisons in `async_fs_dir_comparison`; `write`, `append`, `metadata`, `rename`, `remove_file`, and directory operations delegate to PAL platform operations, and `copy` delegates to the PAL platform copy operation instead of allocating a user-space transfer buffer.
- Moirai-owned network facade value semantics: TCP loopback read/write, write-side shutdown EOF, bounded write backpressure, read readiness before peer data, pending-read cancellation safety, and UDP loopback send/receive are covered by value-sensitive module tests; TCP loopback accept/echo, persistent stream echo, write shutdown, write backpressure, read readiness, and pending-read cancellation safety have Tokio comparison rows in `async_tcp_comparison`, `async_tcp_backpressure_comparison`, `async_tcp_readiness_comparison`, and `async_tcp_cancel_safety_comparison`; UDP loopback receive has a Tokio comparison row in `async_udp_comparison`.
- Bounded channel transport: `moirai_core::channel::mpmc` has value-checked `bounded_channel_matrix` rows against `tokio::sync::mpsc`, with executable `moirai_mpmc` and `tokio_mpsc` row names.
- PAL async file facade: `moirai-pal::fs::AsyncFile` has value-semantic roundtrip, seek, metadata, read-to-end, platform-write, platform-append, path-metadata, path-rename, path-remove, directory create/remove, recursive directory create/remove, and platform-copy tests.
- PAL async socket self-wake fallback: `moirai-pal::net::AsyncTcpListener`, `AsyncTcpStream`, and `AsyncUdpSocket` self-wake when no active reactor is installed, and register reactor wakers when one is active.
- PAL reactor task-handle completion: `IoReactor::spawn` publishes completion through per-task atomic state and wakes the awaiting `TaskHandle`.
- PAL reactor task queue avoids dynamic future dispatch: queued reactor futures use bounded inline storage plus monomorphized poll/drop functions instead of `Pin<Box<dyn Future<Output = ()>>>`; oversized futures use a typed boxed fallback stored behind the same monomorphized path.
- PAL platform reactor dispatch is static: `create_reactor` returns the compile-target `PlatformReactor` type instead of `Box<dyn Reactor>`.
- Public scheduler task dispatch is concrete: `moirai-core::ScheduledTask` uses `INLINE_SCHEDULED_TASK_WORDS` inline storage with monomorphized execute/drop/context functions, and `WorkStealingScheduler` plus `NumaAwareScheduler` queue `ScheduledTask` values instead of `Box<dyn BoxedTask>`.
- Standalone scheduler work-stealing storage is contiguous: `moirai-scheduler::ChaseLevDeque` stores `ScheduledTask` values in `UnsafeCell<MaybeUninit<T>>` ring slots instead of allocating one `Box<T>` per queued item. Reclamation is monomorphized by policy: default `QuiescentReclaim` uses zero-sized state and guard; opt-in `SharedEpochReclaim` uses one active-access counter to block shared reclamation while push, pop, or steal may hold an array pointer.
- Standalone deque reclamation policy overhead is measured by `thread_schedule_comparison -- standalone_deque_reclaim_policy`; the diagnostic keeps `QuiescentReclaim` as the production default and `SharedEpochReclaim` as an explicit opt-in policy.
- PAL epoll reactor wake: Linux `EpollReactor::wake` uses an `eventfd` registered with epoll instead of a no-op wake path.
- Async executor queueing: heterogeneous queued futures use monomorphized poll/drop function pointers instead of `dyn Future`.
- Iterator support infrastructure: `ThreadPool` queues `ErasedThreadJob` values instead of boxed `dyn FnOnce` jobs.
- Iterator channel fusion infrastructure: splitters and mergers store one concrete `FusableChannel` type per instance instead of boxed channel trait objects.
- Iterator streaming infrastructure: `StreamingIter<T, F>` stores a concrete producer and `VecDeque` FIFO buffer; the unused boxed-future base trait is removed.
- Iterator SIMD surface is generic: `SimdSliceIter<T>` is backed by the sealed `SimdScalar` contract, stale backup source is removed, and `iter_simd_comparison` verifies generic add and dot rows against scalar references.

Drop-in API compatibility with every Tokio I/O type or every Rayon `ParallelIterator` adapter is not part of this scheduler audit. Those are ecosystem extension goals, not active gaps in the unified scheduler comparison.

## Verdict

No active comparison gap remains in the official scheduler/result-handle/indexed-reduction scope. Each accepted comparison has:

- A real Moirai API path.
- A real Tokio or Rayon reference path where the semantics match.
- A value assertion before timing.
- A bounded Criterion benchmark target.
- A `benchmark_contracts` test that protects benchmark shape, value semantics, and source invariants.

The 2026-05-25 audit pass keeps that verdict unchanged for covered semantics and records the remaining work as deferred ecosystem or cleanup gaps. These gaps are not scheduler comparison failures because they either require non-equivalent API compatibility work or affect inactive legacy files rather than compiled runtime dependency boundaries.

## Tokio Comparison Matrix

| Capability | Moirai path | Tokio reference | Verification | Status |
| --- | --- | --- | --- | --- |
| Ready result task | `Moirai::spawn_fn` plus `TaskHandle::join` | `tokio::spawn` plus `JoinHandle::await` | `public_result_handle_comparison`, `result_handle_diagnostics`, `benchmark_contracts` | Covered |
| Captured ready result task | `Moirai::spawn_fn` with captured data | `tokio::spawn` with captured data | `public_result_handle_comparison`, `result_handle_diagnostics`, `benchmark_contracts` | Covered |
| Oversized captured result task | boxed inline trampoline behind `InlineJob` | `tokio::spawn` with equivalent captured data | `public_result_handle_comparison`, `result_handle_diagnostics`, `benchmark_contracts` | Covered |
| Ready async result task | `Moirai::spawn_async` plus `TaskHandle::join` | ready `tokio::spawn` future | `public_result_handle_comparison`, async state contract checks | Covered |
| Wake/requeue async result task | wake-once `Moirai::spawn_async` future | wake-once `tokio::spawn` future | `public_result_handle_comparison`, async requeue tests | Covered |
| Ready async iterator pipeline | `moirai-iter::AsyncIterator` map/filter/materialize | Tokio `JoinSet` per-item fan-out over the same ready work | `async_iterator_comparison`, `benchmark_contracts` | Covered |
| Async iterator logical-window pipeline | `moirai-iter::AsyncIterator` map/take/skip/materialize | Tokio `JoinSet` per-item fan-out over the same transformed retained window | `async_iterator_comparison`, `benchmark_contracts` | Covered |
| Async iterator enumerate/zip pipeline | `moirai-iter::AsyncIterator` map/zip/enumerate/materialize | Tokio `JoinSet` fan-out over both inputs followed by ordered zip/enumerate checksum | `async_iterator_comparison`, `benchmark_contracts` | Covered |
| Bounded async iterator pipeline | `moirai-iter::AsyncParallelIterator` bounded `par_map`/`par_filter` | Tokio bounded `JoinSet` fan-out over the same one-pending-poll work | `async_iterator_comparison`, bounded in-flight unit tests, `benchmark_contracts` | Covered |
| Native I/O extension futures | `moirai_async::io::{AsyncReadExt, AsyncWriteExt}` | Tokio-style `read_exact`, `write_all`, and `shutdown` extension semantics | `moirai-async::io` value tests, `async_tcp_comparison`, `benchmark_contracts` | Covered native trait slice |
| Tokio I/O trait compatibility | `TokioCompat<T>` and `MoiraiCompat<T>` transparent wrappers | `tokio::io::{AsyncRead, AsyncWrite}` adapters under the `tokio-compat` feature | `moirai-async::io` feature-gated value tests, `async_io_compat_comparison`, `benchmark_contracts` | Covered trait slice |
| Tokio file facade read | `moirai_async::fs::read` | `tokio::fs::read` over the same prepared 64 KiB file | `async_fs_comparison`, file facade value tests, `benchmark_contracts` | Covered |
| Tokio file facade write | `moirai_async::fs::write` through PAL platform write | `tokio::fs::write` over the same prepared 64 KiB payload | `async_fs_comparison`, `async_fs_write_file`, file facade write tests, `benchmark_contracts` | Covered |
| Tokio file facade append | `moirai_async::fs::append` through PAL platform append | `tokio::fs::OpenOptions::append` plus `write_all` over the same prepared prefix and 64 KiB payload | `async_fs_comparison`, `async_fs_append_file`, file facade append tests, `benchmark_contracts` | Covered |
| Tokio file facade metadata | `moirai_async::fs::metadata` through PAL platform metadata | `tokio::fs::metadata` over the same prepared 64 KiB file | `async_fs_comparison`, `async_fs_metadata_file`, file facade metadata tests, `benchmark_contracts` | Covered |
| Tokio file facade rename | `moirai_async::fs::rename` through PAL platform rename | `tokio::fs::rename` over prepared 64 KiB source files | `async_fs_comparison`, `async_fs_rename_file`, file facade rename tests, `benchmark_contracts` | Covered |
| Tokio file facade remove | `moirai_async::fs::remove_file` through PAL platform remove | `tokio::fs::remove_file` over prepared 64 KiB source files | `async_fs_comparison`, `async_fs_remove_file`, file facade remove tests, `benchmark_contracts` | Covered |
| Tokio file facade copy | `moirai_async::fs::copy` through PAL platform copy | `tokio::fs::copy` over the same prepared 64 KiB file | `async_fs_comparison`, `async_fs_copy_file`, file facade copy tests, `benchmark_contracts` | Covered |
| Tokio directory facade create/remove | `moirai_async::fs::{create_dir, remove_dir}` through PAL platform directory operations | `tokio::fs::{create_dir, remove_dir}` over one empty directory | `async_fs_dir_comparison`, `async_fs_create_remove_dir`, directory facade tests, `benchmark_contracts` | Covered |
| Tokio directory facade recursive create/remove | `moirai_async::fs::{create_dir_all, remove_dir_all}` through PAL platform directory operations | `tokio::fs::{create_dir_all, remove_dir_all}` over one nested tree with a verified marker file | `async_fs_dir_comparison`, `async_fs_create_remove_dir_all`, directory facade tests, `benchmark_contracts` | Covered |
| Tokio TCP loopback accept/echo | `moirai_async::net::TcpListener` and `TcpStream` | `tokio::net::TcpListener` and `TcpStream` over the same request/echo payloads | `async_tcp_comparison`, `async_tcp_loopback_echo`, network facade loopback tests, `benchmark_contracts` | Covered facade slice |
| Tokio TCP persistent stream echo | `moirai_async::net::TcpStream` over an established loopback stream | `tokio::net::TcpStream` over an established loopback stream with the same request/echo payloads | `async_tcp_comparison`, `async_tcp_stream_echo`, network facade loopback tests, `benchmark_contracts` | Covered facade slice |
| Tokio TCP write shutdown | `moirai_async::net::TcpStream` write-side shutdown | `tokio::net::TcpStream` write-side shutdown over the same payload and EOF observation | `async_tcp_comparison`, `async_tcp_write_shutdown`, network facade shutdown test, `benchmark_contracts` | Covered facade slice |
| Tokio TCP write backpressure | `moirai_async::net::TcpStream::from_std` plus `poll_write` | `tokio::net::TcpStream::from_std` plus `poll_write` over bounded socket buffers | `async_tcp_backpressure_comparison`, `async_tcp_write_backpressure`, network facade backpressure test, `benchmark_contracts` | Covered facade slice |
| Tokio TCP read readiness | `moirai_async::net::TcpStream::from_std` plus `poll_read` pending-before-data and payload-ready observation | `tokio::net::TcpStream::from_std` plus `readable` / `try_read` | `async_tcp_readiness_comparison`, `async_tcp_read_readiness`, network facade readiness test, `benchmark_contracts` | Covered facade slice |
| Tokio TCP pending-read cancellation safety | borrowed `moirai_async::io::AsyncReadExt::read_exact` future dropped after `Poll::Pending` | borrowed `tokio::io::AsyncReadExt::read_exact` future dropped after `Poll::Pending` | `async_tcp_cancel_safety_comparison`, `async_tcp_pending_read_cancel_safety`, network facade cancellation-safety test, `benchmark_contracts` | Covered facade slice |
| Tokio UDP loopback receive | `moirai_async::net::UdpSocket::recv_from` | `tokio::net::UdpSocket::recv_from` over the same loopback datagram payload | `async_udp_comparison`, network facade loopback tests, `benchmark_contracts` | Covered facade slice |
| Bounded channel transfer | `moirai_core::channel::mpmc` over fixed producer/capacity matrix | `tokio::sync::mpsc::channel` over the same producer/capacity matrix | `channel_matrix`, `benchmark_contracts` | Covered |
| Batch quiescence | `Moirai::join` after producer submission | runtime completion before continuation | `result_handle_diagnostics`, scheduler join tests | Covered |

## Rayon Comparison Matrix

| Capability | Moirai path | Rayon reference | Verification | Status |
| --- | --- | --- | --- | --- |
| Scoped completion-only fan-out | `Moirai::scope` | `rayon::scope` | `thread_schedule_comparison`, `industry_comparison`, `public_result_handle_comparison`, `benchmark_contracts` | Covered |
| Indexed map/reduce | `Moirai::map_reduce_indexed` | `into_par_iter().map(...).sum()` through a fixed-size Rayon pool | `thread_schedule_comparison`, `industry_comparison`, `example_pattern_comparison`, `benchmark_contracts` | Covered |
| Indexed worker chunking | worker-sized typed chunks with caller participation | Rayon indexed worker pool | `thread_schedule_comparison`, scheduler unit tests | Covered |

## Mixed Tokio/Rayon Comparison Matrix

| Capability | Moirai path | Tokio/Rayon reference | Verification | Status |
| --- | --- | --- | --- | --- |
| Mixed sync completion, async results, and indexed reduction | one `Moirai` runtime using `scope`, `spawn_async`, and `map_reduce_indexed` | Tokio `JoinHandle` async work plus Rayon `scope` and indexed reduction | `thread_schedule_comparison`, `benchmark_contracts` | Covered |

Rayon does not expose a per-task result handle equivalent to `TaskHandle<T>`, so result-handle rows are not compared against Rayon as competitive rows. The audit requires direct Moirai `scope` versus Rayon `scope` rows for completion-only semantics instead.

## Deferred Ecosystem And Cleanup Gap Matrix

| Gap | Current evidence | Required next artifact | Status |
| --- | --- | --- | --- |
| Tokio reactor-native I/O drop-in compatibility | `moirai-async::fs` has covered file facade read, write, append, metadata, rename, remove, copy, and directory-operation benchmarks; `moirai-async::io` has feature-gated Tokio trait wrappers with value and benchmark coverage; `moirai-async::net` has Moirai-owned TCP/UDP loopback value tests, TCP facade benchmarks, and a UDP facade benchmark; PAL socket types register active-reactor wakers and self-wake without an active reactor; PAL file I/O remains a cooperative standard-file facade | File readiness ownership, OS I/O cancellation, and full reactor-native drop-in behavior remain specified under [ADR-006](file:///d:/Moirai/docs/adr.md#adr-006-async-io-compatibility-and-tokio-trait-integration) | Accepted |
| Moirai-owned network facade value semantics | `moirai-async::net` covers TCP loopback read/write, write-side shutdown EOF, bounded write backpressure, read readiness, pending-read cancellation safety, server byte counters, and UDP loopback send/receive plus packet counters; `async_tcp_comparison` compares same-payload TCP loopback accept/echo, persistent stream echo, and write shutdown against Tokio; `async_tcp_backpressure_comparison` compares bounded write backpressure against Tokio; `async_tcp_readiness_comparison` compares read readiness against Tokio; `async_tcp_cancel_safety_comparison` compares pending-read cancellation safety against Tokio; `async_udp_comparison` compares same-payload UDP loopback receive against Tokio | Keep claims limited to the facade slice until reactor-native readiness exists | Covered |
| Rayon adapter surface compatibility | `docs/rayon_adapter_surface_audit.md` maps the current `moirai-iter::parallel` adapter subset, value-preserving reductions, terminal reducers including ordered comparator/key, find-map, position, reverse-order predicate, fallible side-effect, and fallible reduction reducers, borrowed reference materialization adapters, value-tested `enumerate`/`zip`/`filter_map`/`while_some`/`flat_map`/`flatten`/`take`/`skip`/`take_any`/`skip_any`/`chain`/`intersperse`/`rev`/`inspect`/`panic_fuse`/`chunks`/`partition`/`unzip`, indexed runtime boundary, and `ParallelSliceMut` sorting slice-extension boundary | Keep sorting claims limited to value-checked `iterator_adapter_comparison` rows and the dedicated `ParallelSliceMut` sorting boundary | Covered |
| Real-application mixed workload comparison | `real_application_mixed_workload` covers task fan-out, async wait, data-parallel analytics, bounded channel transfer, checksum/value assertions, and same-run Tokio/Rayon reference semantics | Keep benchmark-contract markers active for `moirai_real_app_pipeline`, `tokio_rayon_real_app_pipeline`, and the closed-form checksum verifier | Covered |
| WASM browser event-loop integration | Native PAL file/socket/reactor tests and benchmarks do not compile or execute the `wasm32` reactor path | Specified under [ADR-007](file:///d:/Moirai/docs/adr.md#adr-007-webassembly-browser-event-loop-integration): WebAssembly Browser Event-Loop Integration | Accepted |
| Legacy inactive Tokio test source | `moirai-async/src/sync_old.rs` was not declared by `moirai-async/src/lib.rs`, was not included by path attributes, and only appeared in planning artifacts | Removed inactive legacy source; remaining `moirai-async/src` Tokio mentions are documentation comments about Tokio independence | Closed |
| Runtime dependency boundary | `rayon_tokio_dependencies_stay_out_of_runtime_dependency_sections` verifies Tokio and Rayon stay out of production `[dependencies]` sections and remain comparison-only | Keep benchmark contract active for every Rayon/Tokio audit refresh | Covered |

## Zero-Cost And Zero-Copy Invariants

| Invariant | Mechanism | Executable evidence |
| --- | --- | --- |
| Work shape routing is zero-sized | sealed `WorkClass` markers: `SyncTask`, `AsyncTask`, `BlockingTask` | `work_class_markers_are_zero_sized`, `benchmark_contracts` source checks |
| Scheduler calls are monomorphized | `schedule::<SyncTask, _>`, `schedule::<AsyncTask, _>`, `schedule::<BlockingTask, _>` | scheduler tests and `benchmark_contracts` |
| Indexed chunking uses worker plus caller lanes | chunk caps include `worker_count + 1` because the caller computes one chunk while worker threads execute scheduled chunks | `indexed_reduce_uses_worker_plus_caller_lane`, scheduler tests |
| Small jobs avoid heap allocation | 14-word inline erased `InlineJob` storage within the two-cache-line job footprint | `inline_job_uses_two_cache_line_budget`, `maximum_two_cache_line_job_uses_inline_storage` |
| Oversized jobs avoid dynamic task dispatch | one typed `Box<F>` captured by a monomorphized trampoline closure | `oversized_job_uses_boxed_inline_trampoline`, `benchmark_contracts` |
| Async public handles avoid boxed futures | future and lifecycle state live inline in heap-stable async state | async public-handle state contract checks |
| Async public result sender avoids mutex storage | one-shot sender lives in `UnsafeCell<Option<TaskResultSender<_>>>` under the async poll-owner state machine | async public-handle state contract checks |
| Async public future-present state avoids atomic synchronization | initialized-future ownership is tracked by `UnsafeCell<bool>` under the poll-owner/drop-exclusivity contract | async public-handle state contract checks and async completion diagnostics |
| Async executor queue avoids dynamic future dispatch | `ErasedTaskFuture` owns a heap-stable concrete future and stores monomorphized poll/drop functions | `async_executor_uses_monomorphized_erased_future_queue` |
| Async executor handles avoid mutexed result storage | `AsyncResultSlot<T>` stores one result and one waker behind atomic single-producer/single-consumer states | `async_executor_handle_uses_inline_result_slot` |
| Timeout composition avoids heap pinning | `Timeout<F>` stores `F` inline and projects it while pinned | `timeout_combinator_stores_future_inline` |
| Timer cancellation avoids placeholder wake paths | `TimerWheel` keeps a real cancelled-ID set and removes cancelled entries lazily during expiration polling | `timer_wheel_cancellation_is_real_and_lazy` |
| Native I/O extension futures avoid allocation and type erasure | `ReadExact<'a, R>` stores `&'a mut R`, `&'a mut [u8]`, and an offset; `Shutdown<'a, W>` stores `&'a mut W`; the source contract rejects boxed/type-erased storage | `async_io_extension_futures_are_zero_copy_and_value_semantic` |
| Tokio I/O compatibility wrappers are transparent | `TokioCompat<T>` and `MoiraiCompat<T>` use `#[repr(transparent)]`, `From<T>`, and feature-gated static trait implementations with no default Tokio runtime dependency | `async_io_extension_futures_are_zero_copy_and_value_semantic`, `async_io_compat_comparison` |
| Async file facade avoids obsolete placeholder futures | `moirai-async::fs` exposes direct async facade functions and removes the unused `AsyncFileOp` wrapper | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file write avoids facade handle and sync overhead | `moirai_async::fs::write` delegates to `moirai_pal::fs::write`, which calls platform `std::fs::write` over `C: AsRef<[u8]>` without allocating an intermediate user buffer or constructing facade stats state | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file append avoids facade handle and sync overhead | `moirai_async::fs::append` delegates to `moirai_pal::fs::append`, which uses platform append options and writes `C: AsRef<[u8]>` without constructing facade stats state or unconditionally syncing | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file metadata avoids facade handle overhead | `moirai_async::fs::metadata` delegates to `moirai_pal::fs::metadata`, which calls platform `std::fs::metadata` without opening a facade file handle or constructing stats state | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file rename avoids user-space transfer buffers | `moirai_async::fs::rename` delegates to `moirai_pal::fs::rename`, which calls platform `std::fs::rename` without reading or copying file contents through user-space buffers | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file remove avoids facade handle overhead | `moirai_async::fs::remove_file` delegates to `moirai_pal::fs::remove_file`, which calls platform `std::fs::remove_file` without opening a facade file handle or constructing stats state | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async file copy avoids user-space transfer buffers | `moirai_async::fs::copy` delegates to `moirai_pal::fs::copy`, which calls the platform `std::fs::copy` operation; source contracts reject the prior 64 KiB heap buffer and manual write loop | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async directory facade avoids direct async-layer platform ownership | `moirai_async::fs::{create_dir, create_dir_all, remove_dir, remove_dir_all}` delegate to PAL directory operations and remain direct monomorphized generic path functions | `async_file_facade_is_value_semantic_and_benchmarked_against_tokio` |
| Async network facade has value-sensitive socket tests | TCP and UDP loopback tests assert payloads and byte/packet counters through Moirai-owned APIs | `async_network_facade_has_loopback_value_tests_and_audited_boundary` |
| TCP shutdown is not a no-op | `TcpStream::poll_shutdown` delegates to PAL `shutdown_write`, which calls `StdTcpStream::shutdown(Shutdown::Write)`; loopback tests assert peer EOF after payload | `async_network_facade_has_loopback_value_tests_and_audited_boundary` |
| TCP write backpressure is observable | Bounded socket-buffer tests call `poll_write` until `Poll::Pending`, assert positive progress before backpressure, and bound total writes before `Pending` | `async_network_facade_has_loopback_value_tests_and_audited_boundary` |
| TCP read readiness is observable | Read-readiness tests call `poll_read` before peer data, assert `Poll::Pending`, release the peer, and assert the exact payload bytes after readiness | `async_network_facade_has_loopback_value_tests_and_audited_boundary` |
| TCP pending-read cancellation is facade-safe | Pending read-future tests drop a borrowed `read_exact` future before peer data, assert the caller buffer is unchanged, release the peer, and assert exact payload delivery on the same stream | `async_network_facade_has_loopback_value_tests_and_audited_boundary` |
| PAL async file facade has value-sensitive tests | `AsyncFile` roundtrip, seek, metadata, and read-to-end tests assert exact bytes and file length | `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| PAL async sockets do not deadlock without an active reactor | no-active-reactor `WouldBlock` paths call `wake_without_active_reactor`, while active-reactor paths register read/write wakers; `tcp_accept_read_write_self_wakes_without_active_reactor` and `udp_recv_self_wakes_without_active_reactor` cover delayed loopback progress | `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| PAL reactor task handles complete | spawned reactor tasks own per-task `TaskCompletion` state; completed tasks publish with release ordering and `TaskHandle` polls with acquire ordering | `spawned_ready_task_handle_completes_after_iteration` and `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| PAL reactor task queue avoids dynamic future dispatch | `ErasedReactorTaskFuture` stores fitting futures inline in `ReactorTaskFutureStorage`, falls back to `Box<F>` only when the concrete future exceeds the static storage budget, and uses monomorphized poll/drop functions for both paths | `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| PAL platform reactor dispatch is static | `PlatformReactor` is selected by target `cfg`, and `create_reactor` returns the concrete target type | `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| Public scheduler task queues avoid dynamic task dispatch | `ScheduledTask` stores fitting tasks inline, falls back to typed `Box<T>` only for oversized tasks, and exposes concrete scheduler queues in `WorkStealingScheduler` and `NumaAwareScheduler` | `public_scheduler_task_surface_uses_scheduled_task_erasure` |
| Standalone scheduler deque avoids per-item heap nodes | `ChaseLevDeque<T, P>` writes tasks into contiguous `MaybeUninit<T>` ring slots and selects retired backing-array reclamation through a sealed policy. `QuiescentReclaim` has zero-sized state and guard; `SharedEpochReclaim` has one active-access counter and monomorphized guards. | `public_scheduler_task_surface_uses_scheduled_task_erasure`, `chase_lev_deque_*` unit tests, and the `standalone_deque_reclaim_policy` benchmark |
| PAL epoll wake is real | Linux epoll registers an internal `eventfd`, `wake` writes to it, and `poll_events` drains it without emitting a user event | `pal_async_io_facades_have_value_tests_and_self_wake_contract` |
| Iterator thread-pool jobs avoid dynamic dispatch | `ErasedThreadJob` owns a concrete job allocation and stores monomorphized run/drop functions | `iter_thread_pool_uses_monomorphized_erased_jobs` |
| Iterator channel split/merge avoids dynamic dispatch | `ChannelSplitter<T, I, C>` and `ChannelMerger<T, C>` store `Vec<C>` and call `C: FusableChannel<T>` directly | `channel_fusion_uses_typed_channels_without_placeholder_pipeline` and iterator unit tests |
| Iterator streaming avoids dynamic producer dispatch | `StreamingIter<T, F>` stores `F: FnMut() -> Option<T>` and uses `VecDeque<T>` FIFO buffering | `streaming_iter_uses_monomorphized_producer_and_fifo_buffer` and iterator unit tests |
| Iterator base avoids boxed futures | obsolete `base::ExecutionBase` boxed-future trait is removed; active context trait remains `execution::ExecutionBase` | `iterator_base_does_not_expose_boxed_future_execution_trait` |
| Result handles avoid channels | atomic one-shot result slot and inline single waiter | task-handle tests and public-handle benchmarks |
| Transport receive avoids owned decode | `ArchiveView` returns borrowed views over owned message bytes | transport tests and `transport_archive_comparison` |

## Benchmark Coverage

| Target | Purpose |
| --- | --- |
| `thread_schedule_comparison` | scheduler-level scoped ready work, indexed reduction, mixed unified-scheduler work against Rayon/Tokio references, and standalone deque reclamation-policy diagnostics |
| `industry_comparison` | larger public comparison groups with checksum assertions |
| `public_result_handle_comparison` | public `TaskHandle` paths against Tokio `JoinHandle`, plus Rayon scoped completion row |
| `result_handle_diagnostics` | result-slot, scheduler, wrapper, registry, lifecycle timing-policy, and oversized-capture cost isolation |
| `transport_archive_comparison` | borrowed archive receive path against owned-decode references |
| `channel_matrix` | bounded Moirai MPMC channel row against Tokio MPSC channel row |
| `example_pattern_comparison` | value-checked async sleep fanout and Rayon-pattern iterator examples |
| `async_iterator_comparison` | value-checked `moirai-iter::AsyncIterator` ready pipeline against Tokio `JoinSet` fan-out |
| `async_fs_comparison` | value-checked Moirai file facade read, platform-write, platform-append, path-metadata, platform-rename, platform-remove, and platform-copy operations against Tokio `tokio::fs::{read, write, metadata, rename, remove_file, copy}` plus Tokio append-open/write over the same 64 KiB file bytes |
| `async_fs_dir_comparison` | value-checked Moirai directory facade single directory create/remove and recursive directory create/remove operations against Tokio `tokio::fs::{create_dir, remove_dir, create_dir_all, remove_dir_all}` |
| `async_io_compat_comparison` | value-checked feature-gated Tokio trait compatibility wrappers against native Moirai extension futures over the same 4 KiB in-memory read/write payloads |
| `async_tcp_comparison` | value-checked Moirai TCP facade accept/echo, persistent stream echo, and write shutdown against Tokio `tokio::net::TcpListener`/`TcpStream` over the same loopback request, echo, shutdown payload, and EOF observations; the persistent Moirai row uses production `AsyncReadExt::read_exact` and `AsyncWriteExt::write_all` |
| `async_tcp_backpressure_comparison` | value-checked Moirai TCP facade write backpressure against Tokio `tokio::net::TcpStream` over bounded socket buffers and the same 16 KiB write chunks |
| `async_tcp_readiness_comparison` | value-checked Moirai TCP facade read readiness against Tokio `tokio::net::TcpStream` with pending-before-data and same-payload readiness observation |
| `async_tcp_cancel_safety_comparison` | value-checked Moirai TCP facade pending-read cancellation safety against Tokio `tokio::net::TcpStream` with unchanged cancelled buffers and same-payload delivery after release |
| `async_udp_comparison` | value-checked Moirai UDP facade receive against Tokio `tokio::net::UdpSocket::recv_from` over the same loopback datagram bytes |
| `benchmark_contracts` | executable source contracts for value assertions, bounded benchmarks, and architecture invariants |

## Acceptance Criteria

- Every competitive benchmark path asserts a closed-form or reference-computed value before black-boxing.
- Criterion targets use explicit sample, warm-up, and measurement windows.
- Public result handles remain separate from completion-only batch rows.
- Mixed scheduler rows must use the same closed-form result across Moirai and Tokio/Rayon reference paths.
- Tokio and Rayon are benchmark, test, and comparison-example dependencies only; production dependency sections do not import either runtime.
- Hot-path scheduler task dispatch does not reintroduce `Box<dyn FnOnce>`, raw heap-job vtables, boxed futures, or dynamic execution-policy dispatch.
- Scoped scheduler jobs buffer inline `ScheduledJob` values instead of boxed `dyn FnOnce` closures.
- Iterator thread-pool jobs queue `ErasedThreadJob` values with monomorphized run/drop functions instead of `Box<dyn FnOnce>` queue items.
- Iterator channel fusion splitters and mergers store concrete channel types and do not expose placeholder hash distribution or non-executing pipeline stages.
- Iterator streaming producers remain generic and FIFO buffering does not use `Vec::remove(0)`.
- Iterator base APIs do not expose unused `Pin<Box<dyn Future<...>>>` execution traits.
- Timer-wheel cancellation must return value-sensitive cancellation results, suppress canceled waker wakeups, and avoid placeholder false returns.
- Async file facade comparison must assert Moirai bytes and Tokio bytes against the same source bytes before timing for read, write, append, metadata, rename, remove, and copy rows. Async directory facade comparison must assert single directory create/remove state and recursive directory create/remove marker-file state before timing.
- Tokio I/O trait compatibility wrappers must be feature-gated, transparent, value-tested in both directions, and benchmarked against native extension futures over the same payloads before any Tokio trait-compatibility claim.
- Async network facade tests must assert TCP and UDP payload equality plus byte/packet counters; TCP and UDP facade benchmarks must assert Moirai and Tokio payload equality before timing; TCP write-backpressure benchmarks must assert both paths write positive bytes before timing; TCP read-readiness benchmarks must assert `Poll::Pending` before peer data and exact payload bytes after release; TCP pending-read cancellation benchmarks must assert unchanged caller buffers after dropping a pending read future and exact payload bytes after release; these tests do not imply Tokio reactor-native drop-in compatibility.
- Bounded channel matrix rows must assert the same closed-form transferred-item checksum before timing each Tokio MPSC and Moirai MPMC path.
- PAL async socket no-active-reactor fallback must wake the current task before returning `Pending`, preventing `block_on` deadlock on delayed readiness.
- PAL reactor task handles must return `Ready(())` after the spawned task completes; tests must reject placeholder pending-only handles.
- PAL reactor task queues must not use `Pin<Box<dyn Future<Output = ()>>>`; heterogeneous queued futures must use bounded inline storage and monomorphized poll/drop function pointers, with typed boxed fallback only for oversized futures.
- PAL platform reactor selection must be static for the compile target and must not return `Box<dyn Reactor>`.
- Public scheduler task APIs must not expose `Box<dyn BoxedTask>`, `dyn Scheduler`, `TaskSlot`, or `Pin<Box<dyn Future>>`; heterogeneous scheduler work must flow through `ScheduledTask`.
- Standalone scheduler deque storage must not reintroduce `Box<[AtomicPtr<T>]>`, `Box::into_raw(Box::new(item))`, or `Box::from_raw(item_ptr)` per-item queue nodes.
- Standalone deque reclamation-policy diagnostics must assert the drained sum for quiescent and shared epoch policy rows before timing.
- Work-class routing remains sealed, zero-sized, and monomorphized through `WorkClass` marker types.
- Documentation, backlog, checklist, changelog, performance results, and benchmark contracts reference the same active comparison scope.

## Latest Measurement Note

The 2026-05-23 `example_pattern_comparison -- example_rayon_patterns` run measures Moirai indexed reduction at 330.64-351.94 µs versus fixed-pool Rayon at 380.51-403.21 µs after indexed chunk caps were corrected to include the caller execution lane. The same-day `industry_comparison -- official_rayon_map_reduce` run keeps Moirai ahead of the fixed-pool Rayon reference at 4,096, 32,768, and 65,536 work items.

The same-day `result_handle_diagnostics -- lifecycle_` run rejects token-carried start-instant lifecycle timing as a production replacement: ready result availability measured 663.08-674.06 ns versus 622.71-633.90 ns for the retained full lifecycle row, while oversized availability measured 755.60-770.63 ns versus 768.33-789.98 ns for the retained row. The executable audit keeps duration observability mandatory and treats atomic-only lifecycle timing as a diagnostic control, not a production policy.

The latest `result_handle_diagnostics -- lifecycle_` run rejects coarse cached-clock lifecycle timing as a production replacement while preserving it as an overhead floor. Ready cached-clock lifecycle availability measured 440.76-459.14 ns versus 615.74-625.31 ns for the retained full lifecycle row. Oversized cached-clock lifecycle availability measured 625.52-682.42 ns versus 749.88-841.37 ns for the retained row. The policy is not production-valid because timestamp precision is bounded by the benchmark clock driver's update cadence.

The latest quick comparison rerun keeps the active Rayon/Tokio gap closed under noisy machine conditions. `public_result_handle_comparison -- public_result_handle_ready` measured Moirai ready, captured, oversized, async-ready, and wake-once result handles at 358.73-375.28 ns, 367.96-390.52 ns, 466.72-488.69 ns, 472.79-510.97 ns, and 513.57-544.64 ns versus Tokio ready, captured, oversized, and wake-once rows at 1.0286-1.2098 µs, 1.1594-1.2275 µs, 1.1370-1.4497 µs, and 1.2109-1.2496 µs. The same run measured Moirai single scoped completion at 294.01-313.60 ns versus Rayon `scope` at 575.96-624.94 ns. `thread_schedule_comparison -- ready_task_schedule` measured Moirai scope at 19.044-19.341 us versus Tokio ready spawn at 89.273-90.520 us and Rayon scope at 80.283-81.728 us. `thread_schedule_comparison -- indexed_reduce_schedule` measured Moirai indexed reduction at 714.22-729.27 ns versus Rayon indexed at 7.7215-8.1235 us. Focused attribution measured async state claims around 5.41-7.23 ns, lifecycle completion at 55.894-56.889 ns, sender-cell send/join at 46.804-47.024 ns, ready completion components at 165.99-166.88 ns, scheduler queue publication at 65.273-65.407 ns, quiescent join spin at 860.54-869.32 ps, pending join spin at 4.2080-4.2403 µs, and contended wake decision at 112.33-119.62 ns. Criterion variance is now attributed to scheduler/lifecycle composition while same-run references remain slower. The production registry remains on `Instant` lifecycle timing; QPC stays benchmark-only after the public oversized-capture A/B regressed.

The registry hot-path diagnostic split now separates lock acquisition, dense block lookup, slot initialization, lifecycle timestamp publication, aggregate mutex registration, and full direct lifecycle before any further registry rewrite. The focused `result_handle_diagnostics` filter measured lock-only at 26.297-31.281 ns, block lookup at 40.774-52.762 ns, slot initialization at 108.28-133.47 ns, timestamp publication at 161.60-177.75 ns, mutex registration at 91.007-103.90 ns, and direct lifecycle at 207.47-332.21 ns. This keeps the active scheduler comparison closed while narrowing the next internal registry target to duration-preserving timestamp publication and slot initialization.

The dense registry state layout now derives `TaskMetadata.id` from the direct lookup id instead of storing a redundant id in every `TaskState`. The feature-gated diagnostic rows after that change measured direct lifecycle at 85.659-90.931 ns, slot initialization at 38.079-41.812 ns, task-state construction at 24.804-25.402 ns, start publication at 28.504-29.063 ns, and completion publication at 29.622-31.151 ns. The active Rayon/Tokio comparison remains closed with the values above.

The timestamp primitive split shows the remaining duration-preserving registry timing cost is precise clock sampling, not release-store publication or duration arithmetic. `registry_elapsed_nanos_since_origin` measured 24.645-24.783 ns, start release publication measured 940.34-945.05 ps, completion release publication measured 563.93-566.76 ps, and duration offset math measured 449.67-453.51 ps. Existing-slot start and completion publication measured 25.159-25.406 ns and 27.402-27.507 ns, while the default `task_scheduling_overhead` gate reran at 531.85-540.70 ns after a noisy preceding same-command run measured 635.02-654.40 ns.

The async future-present flag is now an inline poll-owner flag rather than an atomic. The corrected `result_handle_diagnostics` rows measured future-present drop at 191.60-194.35 ps, full ready-completion components at 150.12-151.23 ns, async-ready at 711.65-739.10 ns, and wake-once at 540.30-577.27 ns. The latest same-run public comparison measured Moirai ready, captured, oversized, async-ready, and wake-once rows at 427.66-476.57 ns, 386.76-414.42 ns, 515.32-556.14 ns, 496.95-545.67 ns, and 531.01-623.14 ns versus Tokio ready, captured, oversized, and wake-once rows at 1.2135-1.3928 us, 1.2970-1.3807 us, 1.5046-1.6921 us, and 1.3826-1.6928 us. The scope rerun measured Moirai at 816.49-942.87 ns versus Rayon at 49.100-111.89 us. Scheduler-level rows measured Moirai scope at 18.385-19.064 us versus Tokio at 5.1317-8.8370 ms and Rayon at 38.031-91.723 us, and Moirai indexed reduction at 1.0172-1.1264 us versus Rayon at 23.985-69.895 us. The default scheduling gate rerun reported no statistically significant change at 658.10-744.73 ns after an initial noisy regression sample, so measurement variance remains a tracked internal risk while the active comparison gap remains closed.

The mixed unified-scheduler comparison covers the user's single-engine requirement directly. `thread_schedule_comparison -- mixed_unified_schedule` combines completion-only sync fan-out, async result handles, and indexed reduction with one closed-form value assertion. The latest rerun measured Moirai at 40.510-41.370 us through one runtime and one scheduler. The Tokio plus Rayon reference row measured 50.147-56.014 us using Tokio for async handles plus Rayon for scoped and indexed work. `thread_schedule_comparison -- real_application_mixed_workload` adds a concrete application-shaped profile with async fan-out, scoped request processing, indexed analytics, bounded channel transfer, and a closed-form checksum. The latest same-run benchmark measured Moirai at 90.956-92.283 us versus Tokio plus Rayon at 108.39-115.36 us.

The standalone deque reclamation diagnostic covers the policy-selection decision without treating it as a Rayon/Tokio competitive row. `thread_schedule_comparison -- standalone_deque_reclaim_policy` measured `QuiescentReclaim` at 2.5038-2.5309 us and `SharedEpochReclaim` at 6.8529-6.8897 us for the same forced-resize, reclaim, and drain workload over 256 values. This keeps shared epoch cleanup available while preserving the zero-sized default policy for production queues.

The timer-wheel cleanup replaces the placeholder `TimerWheel::cancel` false return with lazy cancellation state and value-semantic wake suppression tests. The Tokio-facing timer benchmark `example_pattern_comparison -- example_tokio_fanout` measured Tokio sleep fanout at 15.356-15.597 ms and Moirai async sleep fanout at 15.518-15.636 ms for the same 256-task checksum workload. The intervals overlap; this keeps timer fanout behavior covered while the direct `TimerWheel` contract protects cancellation correctness.

The Rayon adapter transform cleanup adds value-tested `filter_map`, `flat_map`, `inspect`, `panic_fuse`, `chunks`, and `partition` to the already covered `enumerate`, `zip`, `take`, `skip`, `chain`, and `rev` non-indexed adapter subset. The active competitive Rayon benchmark remains the scheduler facade path: `example_pattern_comparison -- example_rayon_patterns` measured fixed-pool Rayon at 1.6761-2.8179 ms and Moirai indexed reduction at 532.31-551.27 µs in the same run. Both rows regressed against local Criterion history, but the same-run comparison remains closed. The direct mixed scheduler rerun measured Moirai at 42.266-42.711 µs and Tokio plus Rayon at 55.762-60.278 µs after the Cargo wrapper timed out behind unrelated package-cache locks.

The iterator base and streaming cleanup removes the unused boxed-future `base::ExecutionBase` trait and the boxed `StreamingIter` producer. The active context trait remains `execution::ExecutionBase`, while `StreamingIter<T, F>` now monomorphizes producer calls and uses `VecDeque` FIFO buffering. `benchmark_contracts` locks both invariants with source checks and value-semantic iterator tests.

The iterator channel-fusion cleanup removes the remaining boxed `FusableChannel` split/merge path and deletes the placeholder hash and non-executing pipeline surface. `cargo bench -p moirai-benchmarks --bench channel_matrix -- p1_c1 --quiet` measured Tokio MPSC at 2.5089-2.6101 ms and Moirai MPMC at 1.4157-1.4504 ms for the same 8,192-item bounded p1/c1 checksum workload. The bounded channel row is now part of the Tokio comparison matrix and benchmark-contract coverage.

The Rayon adapter utility cleanup adds `inspect`, `panic_fuse`, `chunks`, `partition`, `unzip`, `sum`, `product`, `min`, `max`, `min_by`, `max_by`, `min_by_key`, `max_by_key`, `find_last`, `position_first`, `position_any`, `position_last`, `find_map_first`, `find_map_any`, `find_map_last`, `map_with`, `map_init`, `update`, `flatten`, `take_any`, `skip_any`, `intersperse`, `while_some`, `for_each_with`, `for_each_init`, `try_for_each`, `try_for_each_with`, `try_for_each_init`, `try_reduce`, `copied`, and `cloned` to the non-indexed adapter subset. `iterator_adapter_comparison` now measures Moirai ahead of same-run Rayon on indexed, filter/flat, flatten, take/skip-any, stateful map, update, intersperse, while-some, try-for-each, stateful for-each, fallible stateful for-each, try-reduce, chain/rev, inspect/chunks, partition, terminal reducer, ordered reducer, find-map, position, reference materialization, and unzip pipelines: 35.664-35.796 µs versus 318.76-322.01 µs, 22.001-22.292 µs versus 2.9053-3.0355 ms, 108.93-137.47 µs versus 1.2705-1.3079 ms, 27.589-28.097 µs versus 993.67 µs-1.0490 ms, 1.2630-1.3841 ms versus 4.4604-21.486 ms, 35.583-37.854 µs versus 373.83-393.54 µs, 91.120-94.203 µs versus 418.76-433.66 µs, 118.97-188.35 µs versus 363.93-379.84 µs, 142.55-149.28 µs versus 932.60 µs-1.1186 ms, 453.72-518.46 µs versus 7.0571-11.419 ms, 720.44 µs-1.0202 ms versus 5.6971-39.419 ms, 20.183-21.585 µs versus 75.866-79.962 µs, 17.993-18.389 µs versus 76.454-80.386 µs, 31.061-31.810 µs versus 36.916-38.040 µs, 29.242-30.103 µs versus 658.16-693.21 µs, 64.686-65.272 µs versus 218.10-226.27 µs, 179.38-190.67 µs versus 3.3072-5.9357 ms, 77.948-85.530 µs versus 238.34-242.20 µs, 33.601-43.300 µs versus 13.150-41.006 ms, 1.9997-2.0162 ms versus 3.0533-3.1264 ms, and 63.013-63.838 µs versus 648.79-671.82 µs. The mixed unified-scheduler rerun measured Moirai at 42.437-43.169 µs versus Tokio plus Rayon at 48.574-52.167 µs.

The sorting slice-extension boundary is now covered by `ParallelSliceMut` and the value-checked `sorting_comparison` benchmark against Rayon `ParallelSliceMut`. The latest same-run benchmark measured stable sort at 76.225-78.202 µs for Moirai versus 143.38-146.10 µs for Rayon, and unstable sort at 48.838-51.041 µs for Moirai versus 66.725-69.234 µs for Rayon.

The async iterator terminal cleanup removes default-return placeholder futures from `moirai-iter::async_iter` and adds `async_iterator_comparison`. The latest same-run benchmark measured `async_iterator_ready_pipeline/moirai/32768` at 404.46-590.99 µs and `async_iterator_ready_pipeline/tokio_joinset/32768` at 24.904-25.380 ms for an equality-checked ready map/filter pipeline. Async logical-window coverage now includes `AsyncIterator::take` and `AsyncIterator::skip`; `async_iterator_take_skip_pipeline` measured Moirai at 85.602-86.859 µs and Tokio `JoinSet` at 23.593-23.921 ms after asserting equal transformed retained collections. Async logical-position and pairing coverage now includes `AsyncIterator::enumerate` and `AsyncIterator::zip`; `async_iterator_enumerate_zip_pipeline` measured Moirai at 672.68-734.62 µs and Tokio `JoinSet` at 48.260-49.144 ms after asserting equal checksums. The bounded async iterator path now drives `ParAsyncMap`, `ParAsyncFilter`, and `ParAsyncForEach` through bounded in-flight polling; `async_iterator_bounded_yield_pipeline` measured Moirai at 1.9756-1.9836 ms and Tokio `JoinSet` at 9.5598-9.7768 ms for the same equality-checked one-pending-poll map/filter workload.

The async file facade cleanup adds value-semantic file write/read/append/metadata/rename/remove/copy tests and `async_fs_comparison`. The Moirai row now uses `Moirai::block_on` rather than `futures::executor::block_on`, preserving the single-runtime benchmark boundary. `moirai_async::fs::write` delegates to `moirai_pal::fs::write`, which calls platform `std::fs::write` over the caller-provided byte slice and removes the prior facade handle, stats update, write loop, and unconditional `sync_all` path from the convenience write API. `moirai_async::fs::append` delegates to `moirai_pal::fs::append`, which opens the file with platform append options and writes the caller-provided byte slice without the facade handle, stats update, or unconditional `sync_all` path. `moirai_async::fs::metadata` delegates to `moirai_pal::fs::metadata`, which calls platform metadata without opening a facade file handle or constructing stats state. `moirai_async::fs::rename` delegates to `moirai_pal::fs::rename`, which calls platform rename without reading or copying file contents through user-space buffers. `moirai_async::fs::remove_file` delegates to `moirai_pal::fs::remove_file`, which calls platform removal without opening a facade file handle or constructing stats state. `moirai_async::fs::copy` delegates to `moirai_pal::fs::copy`, which calls the platform `std::fs::copy` operation and removes the prior 64 KiB heap buffer plus manual user-space transfer loop. The latest same-run 64 KiB read benchmark measured `async_fs_read_to_end/moirai/65536` at 39.127-45.710 µs and `async_fs_read_to_end/tokio/65536` at 96.964-100.34 µs for the same prepared file after both paths asserted equality against the generated bytes. The latest 64 KiB write benchmark measured `async_fs_write_file/moirai/65536` at 2.8650-3.4698 ms and `async_fs_write_file/tokio/65536` at 2.5939-3.2074 ms after setup asserted destination bytes for both paths. The latest 64 KiB append benchmark measured `async_fs_append_file/moirai/65536` at 272.59-291.93 µs and `async_fs_append_file/tokio/65536` at 190.29-320.18 µs after setup asserted prefix plus appended bytes for both paths. The latest 64 KiB metadata benchmark measured `async_fs_metadata_file/moirai/65536` at 25.187-28.833 µs and `async_fs_metadata_file/tokio/65536` at 85.097-87.725 µs after both paths asserted file type and exact byte length. The latest 64 KiB rename benchmark measured `async_fs_rename_file/moirai/65536` at 603.37 µs-2.0949 ms and `async_fs_rename_file/tokio/65536` at 3.5253-7.3040 ms after per-iteration setup prepared source files and both paths asserted source removal plus destination length. The latest 64 KiB remove benchmark measured `async_fs_remove_file/moirai/65536` at 168.50-193.31 µs and `async_fs_remove_file/tokio/65536` at 189.80-211.05 µs after per-iteration setup prepared source files and both paths asserted removal. The latest 64 KiB copy benchmark measured `async_fs_copy_file/moirai/65536` at 536.26-604.18 µs and `async_fs_copy_file/tokio/65536` at 541.41-716.30 µs after both paths asserted copied byte counts and destination byte equality. This is a covered Moirai file facade slice, not a claim of full Tokio I/O drop-in compatibility.

The async directory facade cleanup routes `moirai_async::fs::{create_dir, create_dir_all, remove_dir, remove_dir_all}` through PAL platform directory operations and adds `async_fs_dir_comparison`. The Tokio directory facade create/remove row compares one directory creation plus removal and asserts directory existence before removal plus absence after removal. The Tokio directory facade recursive create/remove row compares nested directory tree creation plus recursive removal and asserts a marker file's bytes before removing the root. The latest single directory create/remove benchmark measured `async_fs_create_remove_dir/moirai/1` at 228.49-251.78 µs and `async_fs_create_remove_dir/tokio/1` at 275.03-287.74 µs. The latest recursive directory create/remove benchmark measured `async_fs_create_remove_dir_all/moirai/1` at 2.8710-3.1976 ms and `async_fs_create_remove_dir_all/tokio/1` at 3.8355-4.2147 ms. This remains a cooperative facade slice and does not claim reactor-native file readiness or OS-level cancellation.

The native I/O extension cleanup adds `AsyncReadExt::read_exact` and `AsyncWriteExt::shutdown` as borrowing futures over the Moirai traits. `ReadExact` stores the caller buffer directly plus a progress offset, so partial bytes remain in the caller buffer if the future is dropped after a pending poll. The feature-gated Tokio compatibility cleanup makes `TokioCompat<T>` and `MoiraiCompat<T>` transparent conversion wrappers and adds value tests for native Moirai readers/writers through Tokio traits plus Tokio duplex streams through Moirai traits. `async_io_compat_comparison` measured `async_io_compat_read_exact/moirai_native` at 2.5060-2.6553 µs, `async_io_compat_read_exact/tokio_compat` at 2.4962-2.6191 µs, `async_io_compat_write_shutdown/moirai_native` at 179.85-191.55 ns, and `async_io_compat_write_shutdown/tokio_compat` at 186.41-195.91 ns. The persistent TCP benchmark now uses production `MoiraiAsyncReadExt::read_exact` and `MoiraiAsyncWriteExt::write_all` for the Moirai established-stream row.

The async network facade cleanup adds `async_tcp_comparison` for same-payload TCP loopback accept/echo, persistent stream echo, and write shutdown rows against Tokio, `async_tcp_backpressure_comparison` for bounded TCP write backpressure against Tokio, `async_tcp_readiness_comparison` for TCP read readiness against Tokio, `async_tcp_cancel_safety_comparison` for pending-read cancellation safety against Tokio, and `async_udp_comparison` for a same-payload UDP loopback receive row against Tokio. Moirai rows use `Moirai::block_on` where async runtime execution is required and direct readiness polling where the contract is readiness/backpressure observation. The latest TCP accept/echo same-run benchmark measured `async_tcp_loopback_echo/moirai/24` at 294.02-354.85 µs and `async_tcp_loopback_echo/tokio/24` at 323.75-365.72 µs. The latest TCP persistent stream same-run benchmark measured `async_tcp_stream_echo/moirai/24` at 23.946-26.092 µs and `async_tcp_stream_echo/tokio/24` at 42.768-45.817 µs after both client and echo sockets enabled TCP_NODELAY. The latest TCP write-shutdown benchmark measured `async_tcp_write_shutdown/moirai/19` at 26.185-34.695 ms and `async_tcp_write_shutdown/tokio/19` at 21.158-27.122 ms for the same payload and peer EOF observation. The latest bounded TCP write-backpressure benchmark measured `async_tcp_write_backpressure/moirai/16384` at 20.171-61.392 ms and `async_tcp_write_backpressure/tokio/16384` at 16.257-43.003 ms for positive-progress writes until `Poll::Pending`. The latest TCP read-readiness benchmark measured `async_tcp_read_readiness/moirai/5` at 564.43-903.33 µs and `async_tcp_read_readiness/tokio/5` at 474.64-739.83 µs after both paths asserted pending before peer data and exact payload after readiness. The latest TCP pending-read cancellation benchmark measured `async_tcp_pending_read_cancel_safety/moirai/5` at 299.08-340.01 µs and `async_tcp_pending_read_cancel_safety/tokio/5` at 339.36-368.55 µs after both paths dropped a pending borrowed read future, asserted unchanged caller buffers, and then asserted exact payload delivery. The latest UDP same-run benchmark measured `async_udp_loopback_recv_from/moirai/27` at 6.1554-6.4334 µs and `async_udp_loopback_recv_from/tokio/27` at 6.2846-6.4721 µs. This is a covered Moirai-owned network facade value semantics slice covering TCP loopback read/write, TCP write shutdown, TCP write backpressure, TCP read readiness, TCP pending-read cancellation safety, and UDP loopback send/receive, not a claim of Tokio reactor-native I/O drop-in compatibility.
